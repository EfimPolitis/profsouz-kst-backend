model NewsImage {
  newsId  String @map("news_id")
  imageId String @map("image_id")
  image   Image  @relation(fields: [imageId], references: [id], onDelete: Cascade)
  news    News   @relation(fields: [newsId], references: [newsId], onDelete: Cascade)

  @@id([newsId, imageId])
}
